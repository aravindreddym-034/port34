{
  "name": "portfolio-root",
  "version": "1.0.0",
  "description": "Root package for running frontend and backend concurrently",
  "scripts": {
    "dev:frontend": "cd frontend && npm start",
    "dev:backend": "cd backend && ..\\.venv\\Scripts\\python -m uvicorn server:app --host 127.0.0.1 --port 8000 --reload",
    "dev": "npx --yes concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  }
}
